#include<iostream>
#include<fstream>
#include<string>
#include<conio.h>
#include<cctype>
#include<vector>
using namespace std;

int main () {
	
	cout<<"Prueba git bashP"<<endl;
	cout<<"prueba 2"<<endl;
	 
	String nombreArchivo;
	String palabraBuscar;
	Char repeat = ‘s’;
	While (repetir == ‘s’ || repetir == ‘S’) {
		cout <<"ingrese el nombre del archivo"<<nombreArchivo<<endl;
		Ifstream archivo nombreArchivo);
		if (¡archivo.is_open) {
		Cerr << “Error al abrir el archivo.” << endl;
		Return 1;
 }
	
	
	getch();
	return 0;
}